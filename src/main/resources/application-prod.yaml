spring:
  datasource:
    url: jdbc:postgresql://postgres_server:${POSTGRES_PORT}/postgres
    username: postgres
    password: platzi
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true
    show-sql: true